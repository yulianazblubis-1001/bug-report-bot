# Rize Report Bot — WhatsApp to Slack Reporter

## Overview
A WhatsApp chatbot that guides Rize.farm agronomists through bug reports, admin requests, and credit limit top-up requests using Claude AI for all conversation flows. The bot speaks casual Indonesian, enforces mandatory fields, and posts professional Slack cards.

## Architecture
- **Frontend**: React + Vite dashboard at `/` showing bot status, activity logs, configuration
- **Backend**: Express server handling WATI webhooks (`/webhook`, `/api/bot/webhook`), Slack events (`/slack-events`), sheet updates (`/sheet-update`), and API routes (`/api/bot/*`)
- **Bot Engine**: Claude AI for all report types (bug, admin, credit limit top-up)

## Key Files

### Server / Bot
| File | Purpose |
|------|---------|
| `server/routes.ts` | Express routes: webhook, slack-events, sheet-update, API endpoints |
| `server/bot/session.ts` | In-memory session store with 30-min TTL, conversation history, Slack mapping |
| `server/bot/router.ts` | Message routing + conversation flow (SELECT_TYPE → SELECT_ADMIN_TYPE → SELECT_CREDIT_TYPE → COLLECTING → CONFIRMING) |
| `server/bot/activityLog.ts` | In-memory activity log for dashboard |
| `server/bot/services/claude-agent.ts` | Claude Sonnet 4 agent — evaluates bug/admin/creditTopUp reports with distinct system prompts |
| `server/bot/services/wati.ts` | WATI API wrapper (sendSessionMessage with query param) |
| `server/bot/services/slack.ts` | Slack Block Kit formatter, credit limit blocks, thread replies, emoji reactions |
| `server/bot/services/google-sheets.ts` | Google Sheets API — append/update/find credit limit rows (Replit Google Sheets integration) |
| `server/bot/services/google-drive.ts` | Google Drive API — upload documents, set permissions (Replit Google Drive integration) |
| `server/bot/services/reportCounter.ts` | **(NEW)** Atomic per-day, per-type counter for Report Number generation. Resets at 00:00 WIB. Persists to `report_counter.json` or `counters` Sheet tab. |
| `server/bot/services/reportRegistry.ts` | **(NEW — REQUIRED FOR RESOLUTION FIX)** Persists `{slack_message_ts → report_number, phone, name, type}` mapping in Google Sheets tab `reports_registry`. Used by resolution handler to include Report Number in DONE/APPROVED/REJECTED WhatsApp messages. |
| `server/bot/whitelist.ts` | Phone whitelist from agronomist database |
| `server/bot/agronomist-database.json` | 71 agronomist profiles (name, area, email, phone) |
| `google-apps-script.js` | Google Apps Script for Sheet onEdit trigger → POST /sheet-update |

### Frontend
| File | Purpose |
|------|---------|
| `client/src/pages/dashboard.tsx` | Main dashboard page |
| `client/src/App.tsx` | App router |

## Report Types

### 1. Bug Report (AI-guided)
- Uses Claude AI to collect info conversationally
- Mandatory: PG Name, Steps to Reproduce, App Version, Platform, Screenshot/Video
- Conditional: Farmer Name, Invoice Number (payment issues)
- Max 3 follow-up questions, translates to English for Slack
- Posts to `SLACK_CHANNEL_BUG`

### 2. Admin Request (AI-guided)
- General admin requests (reset password, etc.)
- Uses Claude AI for collection
- Posts to `SLACK_CHANNEL_ADMIN`

### 3. Credit Limit Top Up (AI-guided)
- Sub-menu: Standard (< 2.5 Ha) or Petani Besar / Large Farmer (> 2.5 Ha s/d 5 Ha)
- Uses Claude AI with specialized system prompt adapted per `creditLimitType`
- Standard: up to 8 follow-ups | Large Farmer: up to 12 follow-ups
- Always required: FG Name, Farmer Name, Land Parcel Size, Current Limit, Requested Top-Up, Credit Type (3 options: Agri Input / Mechanization / Both), Reason
- Conditional (Agri Input): SO Number, Signed SO photo, Farmer holding SO photo
- Conditional (Mechanization): Signed Request Letter photo, Farmer holding Request Letter photo
- If Credit Type = Both: ALL documents from both types required
- Large Farmer additional: Farmer Income Sources, Business Potential, Land Ownership Proof, Collateral Type, Collateral Photo, Collateral Certificate, Credit Limit Request Amount
- Claude does NOT validate amounts — that's Ops Excellence's job
- Documents uploaded to Google Drive → shareable URLs
- Data written to Google Sheets (21 columns, docs collapsed into M-P; full links in Slack card)
- Posts to `SLACK_CHANNEL_CREDIT_LIMIT` with Ops Excellence mentions
- Approval from Google Sheet (onEdit trigger) → POST /sheet-update (with optional SHEET_WEBHOOK_SECRET)
- Engineer resolution: ✅ reaction when processed → WhatsApp notification

## Report Number Generation (NEW)
Every successfully submitted report receives a unique **Report Number** generated at submission time, BEFORE the Slack card is created.

### Format
`YYYYMMDD-TYPE-N`

- `YYYYMMDD` — date the report is created (WIB / Asia/Jakarta timezone)
- `TYPE` — 3-letter code based on user selection:
  - `BUG` → Bug Report
  - `ADM` → Admin Request (general)
  - `CRD` → Credit Limit Top Up (both Standard and Petani Besar)
- `N` — daily incrementing counter, **starts at 1** every day at 00:00 WIB, **increments per type**

### Examples
- `20260421-BUG-1` → first bug report on April 21, 2026
- `20260421-ADM-5` → fifth admin request on April 21, 2026
- `20260421-CRD-2` → second credit limit request on April 21, 2026

### When It's Generated
Report Number is created ONLY after Claude confirms data is sufficient and user types `KIRIM`. If user types `ULANG`, no number is consumed.

**Important**: The `YYYYMMDD` portion uses the **KIRIM submission timestamp** (in WIB), NOT the time the user started the flow. This prevents date confusion when a user starts a flow late at night and submits after midnight.

### Storage
- Counter persisted per `(date, type)` key — survives server restart
- Recommended: store in a small `report_counter.json` file or new Google Sheets tab `counters` (date, type, last_number)
- Atomic increment to prevent duplicate numbers under concurrent submissions

### Where It Appears
1. **Slack card** — labeled `Report Number` field at the top of the card (above other indexed fields)
2. **WhatsApp confirmation reply** to agronomist after submission
3. **WhatsApp resolution notification** when engineer/Ops marks report as done
4. **Google Sheets** (credit limit only) — added as new column `X` (Report Number)

## Card Indexing & Ordering (NEW)
Once Claude confirms data is sufficient, the Slack card fields are arranged in a fixed, predictable order so reviewers can scan quickly:

### Bug Report Card Order
1. Report Number
2. Reporter (name + area)
3. Category
4. Summary (English)
5. Steps to Reproduce
6. App Version + Platform
7. Screenshot/Video link
8. Conditional fields (Farmer Name, Invoice Number)
9. Timestamp (WIB)

### Admin Request Card Order
1. Report Number
2. Reporter (name + area)
3. Category
4. Summary (English)
5. Request details
6. Timestamp (WIB)

### Credit Limit Card Order
1. Report Number
2. Reporter (name + area)
3. FG Name + Farmer Name
4. Land Parcel Size
5. Current Limit + Requested Top-Up
6. Credit Type + Reason
7. SO Number (if Agri Input)
8. Document links (Drive URLs)
9. Large Farmer fields (if applicable)
10. Ops Excellence mentions
11. Timestamp (WIB)

## Slack Card Format
- Bug/Admin: `[Category] Short English summary` (no emojis), with `Report Number` shown as the first field
- Credit Limit: `🏦 CREDIT LIMIT TOP UP REQUEST` with structured fields, doc links, validation flags, and `Report Number` as the first field
- Categories: App Bug, Farmer Data, Payment, Field Task, Account, Carbon/AWD, UI/UX, Admin Request, Other

## Google Sheets Column Structure (Credit Limit — 24 columns A-X)
| Col | Header | Source |
|-----|--------|--------|
| A | Timestamp | Auto (WIB) |
| B | Request ID | Auto UUID |
| C | Reporter Name | Session |
| D | Reporter Phone | Session |
| E | FG | Claude parsedReport |
| F | Farmer | Claude parsedReport |
| G | Land Size Verified | Claude parsedReport |
| H | Current Limit | Claude parsedReport |
| I | Requested Top-Up | Claude parsedReport |
| J | Credit Type | Claude parsedReport |
| K | Reason | Claude parsedReport |
| L | SO Number | Claude parsedReport (Agri Input only) |
| M | Farmer Source of Income & Potential Business | Combined `;`-separated (Large Farmer only) |
| N | Jenis Jaminan - nilai jaminan | Combined `;`-separated (Large Farmer only) |
| O | Doc: Signed SO/Request Letter | Google Drive URL |
| P | Doc: Farmer Holding Document | Google Drive URL |
| Q | Doc: Land Ownership Proof | Google Drive URL (Large Farmer only) |
| R | Doc: Dokumen Jaminan | Google Drive URL (Large Farmer only) |
| S | Status | PENDING → APPROVED/REJECTED → RESOLVED |
| T | Reviewed By | Google Sheet (Ops fills) |
| U | Review Date | Auto |
| V | Rejection Reason | Google Sheet (Ops fills) |
| W | Slack Message TS | System |
| X | Report Number | **(NEW)** Auto-generated `YYYYMMDD-CRD-N` |

## Credit Limit Approval Workflow
1. Agronomist submits via WhatsApp → data written to Google Sheets as PENDING
2. Slack card posted to credit limit channel, mentioning Ops Excellence team
3. Ops Excellence reviews in Google Sheet, changes Status (col Q) to APPROVED or REJECTED
4. Google Apps Script `onEdit` trigger sends POST to `/sheet-update`
5. APPROVED: `:git-approved:` emoji on Slack, thread reply tagging engineers
6. REJECTED: `:rejected:` emoji on Slack, WhatsApp notification to reporter with reason
7. Engineer reacts ✅ or :done: on Slack → Sheet updated to RESOLVED, WhatsApp notification sent

## Environment Variables
- `WATI_API_ENDPOINT` — WATI API base URL
- `WATI_TOKEN` — WATI Bearer token
- `SLACK_WEBHOOK_BUG` — Slack webhook for bug reports (fallback)
- `SLACK_WEBHOOK_ADMIN` — Slack webhook for admin requests (fallback)
- `SLACK_BOT_TOKEN` — Slack Bot User OAuth Token (xoxb-) for Web API
- `SLACK_CHANNEL_BUG` — Slack channel ID for bug reports (C085L46D50A)
- `SLACK_CHANNEL_ADMIN` — Slack channel ID for admin requests (C0766PKUK1N)
- `SLACK_CHANNEL_CREDIT_LIMIT` — Slack channel ID for credit limit requests (C0766PKUK1N)
- `SLACK_MENTION_OPS` — Comma-separated Slack user IDs for Ops Excellence mentions
- `GOOGLE_SHEETS_ID` — Google Sheets spreadsheet ID for credit limit data
- `GOOGLE_DRIVE_FOLDER_ID` — Google Drive folder ID for document uploads
- `ANTHROPIC_API_KEY` — Claude AI key for conversation agent
- `SLACK_SIGNING_SECRET` — Slack signature verification
- `SESSION_SECRET` — Express session secret

## Slack Integration
- Primary: Uses Slack Web API (`chat.postMessage`) with `SLACK_BOT_TOKEN`
- Slack Events endpoint: `/api/bot/slack-events` and `/slack-events`
- Handles: `reaction_added` (bug/admin done/solve, credit limit engineer resolution)
- Reactions API for approval/rejection emojis (:git-approved:, :rejected:)
- Bot needs OAuth scopes: chat:write, reactions:read, reactions:write, files:write, users:read
- Bot must be invited to all channels
- Slack app needs Event Subscriptions: reaction_added, message.channels

## Google Integrations
- Google Sheets: Uses Replit Google Sheets connector (OAuth via Replit integrations)
- Google Drive: Uses Replit Google Drive connector (OAuth via Replit integrations)
- Never cache the client — tokens expire, always get fresh client

## How It Works
1. Agronomist sends WhatsApp message via WATI
2. Whitelist check — rejects unregistered numbers, auto-identifies from database
3. User picks Bug Report (1) or Admin Request (2)
4. If Admin Request → sub-menu: General (1) or Credit Limit Top Up (2)
5. If Credit Limit → sub-menu: Standard (1) or Petani Besar/Large Farmer (2)
6. All types: Claude AI evaluates, enforces mandatory fields, asks follow-ups
7. Credit Limit: Claude uses specialized prompt per creditLimitType, 8 follow-ups (standard) or 12 (large farmer)
7. When ready, shows summary for confirmation (KIRIM to submit, ULANG to restart)
8. On submit:
   - **Generate Report Number** (`YYYYMMDD-TYPE-N`) via atomic counter
   - Index/order card fields per the Card Indexing rules
   - Posts Slack card with Report Number as first field
   - Uploads docs to Google Drive (credit limit only)
   - Writes Google Sheets including Report Number in col X (credit limit only)
   - Sends WhatsApp confirmation to agronomist:
     - Bug: `Bug Report berhasil dikirim! Report Number {YYYYMMDD-BUG-N}`
     - Admin: `Admin Request berhasil dikirim! Report Number {YYYYMMDD-ADM-N}`
     - Credit: `Credit Limit Top Up berhasil dikirim! Report Number {YYYYMMDD-CRD-N}`
9. Credit limit approval: Ops edits Sheet → Apps Script triggers /sheet-update → Slack reactions + WhatsApp (include Report Number in WhatsApp message)
10. Engineer resolves with ✅ reaction → Sheet updated, WhatsApp notification sent with Report Number:
    - Example: `Laporan kamu dengan Report Number {YYYYMMDD-BUG-N} sudah ditandai DONE oleh tim`

## Report Number in Resolution Message (IMPLEMENTATION REQUIRED)

### Problem Statement
The resolution WhatsApp message (sent when engineer/support marks the ticket as DONE via ✅ reaction) MUST include the Report Number. Currently it does not.

### Expected Behavior
When engineer reacts with ✅ or `:done:` on a Slack card, the agronomist MUST receive a WhatsApp message containing the exact Report Number from that card.

**Required message format (Indonesian + English translation):**
```
Halo {ReporterName}! Laporan kamu dengan Report Number {YYYYMMDD-TYPE-N} sudah ditandai DONE oleh tim. Masalahnya sudah diperbaiki, silakan coba lagi.

(Your report with Report Number {YYYYMMDD-TYPE-N} has been marked DONE by the team. The issue has been fixed, please try again.)
```

For credit limit resolution:
```
Halo {ReporterName}! Credit Limit Top Up dengan Report Number {YYYYMMDD-CRD-N} sudah diproses oleh tim.
```

### Implementation Instructions for Replit

#### Step 1: Create Report Registry Service
Create new file: `server/bot/services/reportRegistry.ts`

Purpose: persist Report Number and reporter info so resolution handler can look it up later (sessions expire after 30 min but tickets may be resolved hours/days later).

**Storage mechanism**: Use Google Sheets tab named `reports_registry` in the same spreadsheet as credit limit (survives Replit restarts, no file system dependency).

**Schema** (Google Sheets tab `reports_registry`):
| Col | Header | Type |
|-----|--------|------|
| A | slack_message_ts | string (primary key) |
| B | slack_channel_id | string |
| C | report_number | string (e.g., `20260421-BUG-1`) |
| D | reporter_phone | string |
| E | reporter_name | string |
| F | report_type | string (`bug` / `admin` / `credit`) |
| G | status | string (`PENDING` / `DONE` / `APPROVED` / `REJECTED`) |
| H | created_at | timestamp (WIB) |
| I | resolved_at | timestamp (WIB, null until resolved) |

**Required methods**:
- `set({ messageTs, channelId, reportNumber, phone, name, type })` → append new row
- `get(messageTs)` → find row by message_ts, return object or null
- `markResolved(messageTs)` → update status to DONE, fill resolved_at

#### Step 2: Update Submission Handler (`server/routes.ts` + `server/bot/router.ts`)
After posting Slack card successfully, BEFORE responding to WhatsApp:

```
// Pseudocode — after slack.postMessage returns { ts, channel }
await reportRegistry.set({
  messageTs: slackResponse.ts,
  channelId: slackResponse.channel,
  reportNumber: generatedReportNumber,  // from reportCounter
  phone: session.phone,
  name: session.reporterName,
  type: session.reportType,  // 'bug' | 'admin' | 'credit'
});
```

This MUST happen for ALL three report types (bug, admin, credit).

#### Step 3: Update Resolution Handler (`server/routes.ts` `/slack-events` endpoint)
In the `reaction_added` handler for ✅ / `:done:` reactions:

```
// Pseudocode
const { item } = slackEvent;
const messageTs = item.ts;
const channelId = item.channel;

// LOOKUP REPORT NUMBER (this is the missing step)
const report = await reportRegistry.get(messageTs);

if (!report) {
  // Fallback: send message without Report Number + log warning
  await activityLog.warn(`Registry miss for ${messageTs}`);
  await wati.sendSessionMessage(phoneFromSlackThread, FALLBACK_MESSAGE);
  return;
}

// Build message WITH Report Number
const message = `Halo ${report.reporter_name}! Laporan kamu dengan Report Number ${report.report_number} sudah ditandai DONE oleh tim. Masalahnya sudah diperbaiki, silakan coba lagi.`;

await wati.sendSessionMessage(report.reporter_phone, message);
await reportRegistry.markResolved(messageTs);
```

#### Step 4: Update WhatsApp Message Templates
Locate hardcoded resolution templates in codebase (likely in `server/routes.ts` or `server/bot/services/wati.ts`) and replace with the formats specified above. All templates MUST include `{reportNumber}` placeholder.

**Templates to update**:

| Trigger | Old Template (remove) | New Template (use this) |
|---------|----------------------|-------------------------|
| Engineer ✅ on bug card | `Halo {Name}! Laporan kamu sudah ditandai DONE oleh tim. Masalahnya sudah diperbaiki, silakan coba lagi.` | `Halo {Name}! Laporan kamu dengan Report Number {ReportNumber} sudah ditandai DONE oleh tim. Masalahnya sudah diperbaiki, silakan coba lagi.` |
| Engineer ✅ on admin card | (same as above) | (same as above, but works for admin too) |
| Engineer ✅ on credit card (after APPROVED) | `Credit Limit Top Up kamu sudah diproses.` | `Halo {Name}! Credit Limit Top Up dengan Report Number {ReportNumber} sudah diproses oleh tim.` |
| Credit REJECTED (from Sheet) | `Credit Limit Top Up kamu ditolak. Alasan: {reason}` | `Halo {Name}! Credit Limit Top Up dengan Report Number {ReportNumber} ditolak. Alasan: {reason}` |
| Credit APPROVED (from Sheet) | `Credit Limit Top Up kamu disetujui.` | `Halo {Name}! Credit Limit Top Up dengan Report Number {ReportNumber} disetujui oleh tim.` |

#### Step 5: Migration for Existing Tickets (Optional)
For tickets submitted BEFORE this fix that are still open on Slack:
- Registry lookup will return null
- Fallback message (no Report Number) will be sent
- Log warning to activity log
- This is acceptable — do NOT block the resolution flow

### Test Checklist Before Redeploy
- [ ] Submit new bug report → verify row created in `reports_registry` Sheet tab
- [ ] Engineer reacts ✅ on bug card → verify WhatsApp message contains Report Number
- [ ] Engineer reacts ✅ on admin card → verify WhatsApp message contains Report Number
- [ ] Ops approves credit in Sheet → verify APPROVED WhatsApp contains Report Number
- [ ] Ops rejects credit in Sheet → verify REJECTED WhatsApp contains Report Number
- [ ] Engineer reacts ✅ on approved credit card → verify DONE WhatsApp contains Report Number
- [ ] React twice on same card → verify no duplicate WhatsApp sent (check `status = DONE` guard)
- [ ] Registry miss (test with fake message_ts) → verify fallback message sent + warning logged

### Files to Modify
| File | Change |
|------|--------|
| `server/bot/services/reportRegistry.ts` | **CREATE NEW** |
| `server/bot/services/google-sheets.ts` | Add helper for `reports_registry` tab operations |
| `server/routes.ts` | Call `reportRegistry.set()` after Slack post; call `reportRegistry.get()` in reaction handler |
| `server/bot/router.ts` | Ensure submission flow passes Report Number to routes.ts |
| Message template locations | Replace all resolution templates per Step 4 table |

### Files NOT to Modify
- `server/bot/session.ts` — session store stays as-is (30-min TTL is fine for active conversations)
- `server/bot/services/reportCounter.ts` — counter logic already works
- Slack card format — Report Number already appears correctly
- Submission confirmation message — already works correctly


## Technical Notes
- WATI sendSessionMessage uses query param `messageText`, not JSON body
- WATI_TOKEN value already includes "Bearer " prefix — do NOT add another
- Claude Sonnet 4 for all conversation types (bug, admin, creditTopUp)
- Sessions auto-expire after 30 minutes of inactivity
- Trigger keywords: BUG, REPORT, ADMIN, REQUEST, START, MULAI, MENU, LAPOR
- Google Apps Script: paste `google-apps-script.js` content into Sheet's Apps Script editor
- Sheet tab name is "request" (not "Sheet1") — all range references use `request!` prefix
- Apps Script watches col S (19) for status change, tab must be named "request"
